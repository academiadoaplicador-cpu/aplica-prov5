services:
  db:
    image: postgres:16-alpine
    container_name: aplica-pro-db
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-aplica}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-aplica}
      POSTGRES_DB: ${POSTGRES_DB:-aplica_pro}
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./docker/db/init.sql:/docker-entrypoint-initdb.d/01-init.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-aplica} -d ${POSTGRES_DB:-aplica_pro}"]
      interval: 5s
      timeout: 5s
      retries: 10
    restart: unless-stopped

  api:
    profiles:
      - dev
    build:
      context: ./server
      target: development
    container_name: aplica-pro-api
    ports:
      - "${API_PORT:-4000}:4000"
    env_file:
      - path: .env.local
        required: false
    environment:
      NODE_ENV: development
      PORT: 4000
      DATABASE_URL: postgresql://${POSTGRES_USER:-aplica}:${POSTGRES_PASSWORD:-aplica}@db:5432/${POSTGRES_DB:-aplica_pro}
      CORS_ORIGIN: ${CORS_ORIGIN:-http://localhost:3000}
      ADMIN_EMAIL: ${ADMIN_EMAIL:-}
      ADMIN_PASSWORD: ${ADMIN_PASSWORD:-}
      JWT_SECRET: ${JWT_SECRET:-dev-only-aplica-pro-jwt-secret-change-in-production-32chars}
    volumes:
      - ./server:/app
      - api_node_modules:/app/node_modules
    command: sh -c "npm ci && npm run dev"
    depends_on:
      db:
        condition: service_healthy
    restart: unless-stopped

  app:
    profiles:
      - dev
    build:
      context: .
      target: development
    container_name: aplica-pro-dev
    ports:
      - "${APP_PORT:-3000}:3000"
    volumes:
      - .:/app
      - node_modules:/app/node_modules
    env_file:
      - path: .env.local
        required: false
    environment:
      CHOKIDAR_USEPOLLING: "true"
      GEMINI_API_KEY: ${GEMINI_API_KEY:-}
      APP_URL: ${APP_URL:-http://localhost:3000}
      VITE_API_PROXY: http://api:4000
    command: sh -c "npm install && npm run dev"
    depends_on:
      - api
    restart: unless-stopped

  app-prod:
    profiles:
      - prod
    build:
      context: .
      target: production
      args:
        GEMINI_API_KEY: ${GEMINI_API_KEY:-}
    container_name: aplica-pro-prod
    env_file:
      - path: .env.production
        required: false
    environment:
      NODE_ENV: production
      PORT: 4000
      DATABASE_URL: postgresql://${POSTGRES_USER:-aplica}:${POSTGRES_PASSWORD:-aplica}@db:5432/${POSTGRES_DB:-aplica_pro}
      CORS_ORIGIN: ${CORS_ORIGIN:-https://app.aplicapro.com.br}
      JWT_SECRET: ${JWT_SECRET:?Defina JWT_SECRET com pelo menos 32 caracteres}
      ADMIN_EMAIL: ${ADMIN_EMAIL:-}
      ADMIN_PASSWORD: ${ADMIN_PASSWORD:-}
      ENABLE_ADMIN_BOOTSTRAP: ${ENABLE_ADMIN_BOOTSTRAP:-false}
      ADMIN_BUSINESS_NAME: ${ADMIN_BUSINESS_NAME:-Gestão do Sistema}
    expose:
      - "80"
    depends_on:
      db:
        condition: service_healthy
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "wget -qO- http://127.0.0.1:80/api/health 2>/dev/null | grep -q '\"status\":\"ok\"' || exit 1",
        ]
      interval: 15s
      timeout: 10s
      retries: 8
      start_period: 90s
    restart: unless-stopped

volumes:
  node_modules:
  api_node_modules:
  postgres_data:
